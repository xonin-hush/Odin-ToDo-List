import { testing } from "./ToDo"
import {format, compareAsc} from "date-fns";
import { DisplayProjects } from "./projects";
format(new Date(2014, 1, 11), "MM/dd/yyyy");
let date=new Date(2004, 9, 22)
console.log(date)
